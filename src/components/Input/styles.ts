import styled from 'styled-components';

export const Container = styled.input`
  padding-left: 1rem;
  flex: 1;
  height: 3rem;

  font-size: 1.2rem;

  border: 1px solid var(--cll-complements);
  border-radius: var(--boxes-radius);
`;
